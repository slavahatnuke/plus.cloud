"use strict";

module.exports = class DeployService {
    constructor(Options) {
        this.Options = Options;
    }

    init() {
        return new Promise((resolve, reject) => {
            resolve()
        });
    }

    deploy() {
        return new Promise((resolve, reject) => {
            resolve()
        });
    }
};
